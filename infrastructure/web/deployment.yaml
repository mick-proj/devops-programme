apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: robot-shop
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
        prometheus: scrape
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: web
          image: ghcr.io/mick-proj/devops-programme/web:bcfbb5d3b9e90eb35332e797fbf28b3c71bc464e
          ports:
            - containerPort: 8080
          env:
            - name: USER_HOST
              value: "localhost"
            - name: SHIPPING_HOST
              value: "localhost"
            - name: PAYMENT_HOST
              value: "localhost"
            - name: RATINGS_HOST
              value: "localhost"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
